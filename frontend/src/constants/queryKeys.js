/**
 * TanStack Query key definitions
 * Guarantees type-safe and consistent caching keys across the app
 */

export const QUERY_KEYS = {
  PROFILE: ["profile"],
  ANNOUNCEMENTS: ["announcements"],
  CLUBS: ["clubs"],
  BOOKINGS: ["bookings"],
  EVENTS: ["events"],
};
